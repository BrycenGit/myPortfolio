import styled from "styled-components";

const Contact = () => {
  return (
    <Container>
      <div>Contact</div>
    </Container>
  );
};

export default Contact;

const Container = styled.div``;
