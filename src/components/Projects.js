import styled from "styled-components";

const Projects = () => {
  return (
    <Container>
      <div>Projects</div>
    </Container>
  );
};

export default Projects;

const Container = styled.div``;
